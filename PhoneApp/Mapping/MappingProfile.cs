using AutoMapper;

namespace PhoneApp.Mapping;

public class MappingProfile : Profile {
    public MappingProfile() {

        // CreateMap<Contact, ContactDto>().ForMember(x => );
        // CreateMap<ContactDto, Contact>();
    }
}